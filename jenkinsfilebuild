node {  
    stage('clone code') {
        git 'https://github.com/vamshi-psy/Middleware-DevOps.git'
    }
    stage('build') { 
        bat 'mvn clean install package'
    }
    
    stage('copy to artifactstore'){
        bat 'copy webapp\\target\\webapp.war C:\\artifactstore'
    }
 }
